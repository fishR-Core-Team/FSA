language: r
sudo: false
cache: packages
warnings_are_errors: false

before_install:
  - Rscript -e 'install.packages(c("nlme"))'

after_success:
  - Rscript -e 'covr::coveralls()'

notifications:
  email:
    on_success: change
    on_failure: always
