ToDo list

o can you remove the dependency on drop.levels() from gdata (and use droplevels() from base).

o bcFuns() -- need details in help file.

o dietOverlap -- need details in help file.

o ks2d() ... make one common function for the 1-sample and 2-sample versions.  Do this after you confirm that you are getting correct results.

o Create a function that will construct a histogram from summarized data ... e.g., length frequency data.

O Add Somers/Somers2 to dynamics plotting in vbStarts()

o Add use of manipulate() in RStudio to the dynamic plotting in vbStarts() and srStarts().
