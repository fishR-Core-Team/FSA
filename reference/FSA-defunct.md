# DEFUNCT functions.

These functions were once part of FSA but have now been removed.

## Usage

``` r
bootCase(...)

chooseColors(...)

compIntercepts(...)

compSlopes(...)

diags(...)

filterD(...)

fitPlot(...)

fsaNews(...)

hoCoef(...)

mapvalues(...)

plotBinResp(...)

residPlot(...)

Subset(...)

Schnute(...)
```

## Arguments

- ...:

  Additional arguments to pass through.
